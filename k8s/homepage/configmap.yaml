apiVersion: v1
kind: ConfigMap
metadata:
  name: homepage-config
data:
  widgets.yaml: |
    - kubernetes:
        cluster:
          show: true
          cpu: true
          memory: true
    - datetime:
        text_size: xl
        format:
          dateStyle: long
          timeStyle: short
  
  services.yaml: |
    - Altyapı:
        - ArgoCD:
            icon: argocd.png
            href: http://argocd.homelab.local
            description: GitOps Pipeline
        - K3s Node:
            icon: kubernetes.png
            widget:
              type: kubernetes
              showDetails: true