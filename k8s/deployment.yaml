apiVersion: apps/v1
kind: Deployment
metadata:
  name: oracle-app
  labels:
    app: oracle
spec:
  replicas: 2
  selector:
    matchLabels:
      app: oracle
  template:
    metadata:
      labels:
        app: oracle
    spec:
      containers:
      - name: oracle
        image: yavuzkaramert/homelab-oracle:76b5f55fbf94ba7c038a1f52578fb10865a5263f
        ports:
        - containerPort: 8000
        resources:
          limits:
            memory: "128Mi"
            cpu: "200m"
        env:
        - name: NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName