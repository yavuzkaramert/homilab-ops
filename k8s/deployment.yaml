apiVersion: apps/v1
kind: Deployment
metadata:
  name: oracle-app
  labels:
    app: oracle
spec:
  replicas: 2
  selector:
    matchLabels:
      app: oracle
  template:
    metadata:
      labels:
        app: oracle
    spec:
      containers:
      - name: oracle
        image: yavuzkaramert/homelab-oracle:1ae1568d35c230180a5114742f18581fbebbac52
        ports:
        - containerPort: 8000
        resources:
          limits:
            memory: "128Mi"
            cpu: "200m"
        env:
        - name: NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName