apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-homelab
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hello-homelab
  template:
    metadata:
      labels:
        app: hello-homelab
    spec:
      containers:
      - name: nginx
        image: nginx:alpine
        ports:
        - containerPort: 80